import styled from 'styled-components/native';

import { Touchable, Text } from './strings';

export const STouchable = styled.TouchableOpacity`${Touchable}`;

export const SText = styled.Text`${Text}`;
